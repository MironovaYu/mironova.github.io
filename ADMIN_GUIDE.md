# Инструкция по работе с админ-панелью

## Вход в админ-панель

Откройте в браузере: **http://&lt;IP-адрес&gt;:4343/admin/**

Введите пароль, который был задан при настройке сервера, и нажмите «Войти».

---

## Навигация

В левом меню расположены все разделы:

| Раздел | Что редактирует |
|--------|----------------|
| **Обзор** | Главная страница админки со статистикой |
| **Настройки** | Имя, специализация, ссылки на Telegram и МАКС |
| **Главная** | Все секции главной страницы |
| **Обо мне** | Страница «Обо мне» |
| **Услуги** | Услуги и цены |
| **Контакты** | Страница контактов |
| **Статьи** | Блог: создание, редактирование, удаление |
| **Документы** | Сканы дипломов и сертификатов |

В нижней части меню:
- **Открыть сайт** — откроет публичную версию сайта
- **Опубликовать** — отправит изменения на публичный сайт
- **Выйти** — выход из админки

---

## Разделы

### Настройки (`Настройки`)

Общие данные сайта, которые отображаются в шапке, подвале и мета-тегах:

| Поле | Описание | Пример |
|------|----------|--------|
| Имя | Имя или фамилия в шапке сайта | `Миронова` |
| Роль | Подпись под именем | `психолог` |
| Тэглайн | Строка в подвале | `Психолог · Телесно-ориентированная терапия` |
| Ссылка Telegram | Полная ссылка на Telegram | `https://t.me/mironova_psy` |
| Ссылка МАКС | Ссылка на сервис записи (необязательно) | `https://...` |
| Год копирайта | Год в подвале | `2026` |

---

### Главная страница (`Главная`)

Самый объёмный раздел — содержит все секции главной страницы.

#### 🌟 Героическая секция (первый экран)

| Поле | Описание |
|------|----------|
| **Фото героя** | Основное фото на первом экране (см. [требования к фото](#фото-героя-главная)) |
| Метка | Маленький текст над заголовком (`Психолог`) |
| Заголовок | Главный заголовок. Поддерживает HTML: `<em>гармонию</em>` для курсива, `&nbsp;` для неразрывного пробела |
| Текст | Подзаголовок под основным заголовком |
| Текст кнопки CTA | Текст основной кнопки (`Записаться`) |
| Текст второй кнопки | Текст вторичной кнопки (`Услуги и цены`) |

#### 🆘 С чем помогаю

Список пунктов «Я могу помочь, если вы:»
- Каждый пункт — отдельное текстовое поле
- Кнопка **✕** справа удаляет пункт
- Кнопка **«+ Добавить пункт»** добавляет новый

#### 👤 Превью «Обо мне»

| Поле | Описание |
|------|----------|
| **Фото** | Фото для блока (см. [требования](#фото-превью-обо-мне-главная)) |
| Метка | Маленький текст (`Обо мне`) |
| Заголовок | Заголовок блока |
| Параграфы | Абзацы текста (добавлять/удалять кнопками) |
| Текст кнопки | Надпись на кнопке-ссылке |

#### 📋 Превью услуг

Карточки услуг на главной:
- Для каждой: иконка (эмоджи), название, описание, цена
- Общие поля: метка, заголовок, подзаголовок, текст кнопки

#### 🔄 Как начать работу

4 шага процесса:
- Для каждого шага: заголовок и описание

#### 📢 CTA-секция

Нижний блок «призыв к действию»:
- Заголовок, текст, текст кнопки

**После внесения изменений нажмите «Сохранить всё» внизу формы.**

---

### Обо мне (`Обо мне`)

| Секция | Описание |
|--------|----------|
| **Фото специалиста** | Фото для страницы «Обо мне» (см. [требования](#фото-специалиста-страница-обо-мне)) |
| Основная информация | Имя и специализация |
| Вступительные параграфы | Абзацы текста (добавлять/удалять) |
| Мой подход | 3 пункта: номер, заголовок, текст |
| Образование и опыт | Карточки квалификации: категория, название, описание |
| Принципы работы | 3 карточки: эмоджи, название, текст |
| CTA-секция | Заголовок, текст, кнопка |

---

### Услуги (`Услуги`)

Страница с детальным описанием услуг.

| Поле | Описание |
|------|----------|
| Метка, Заголовок, Подзаголовок | Шапка страницы |
| **Каждая услуга** содержит: | |
| — Название | Заголовок услуги |
| — Описание | Короткий подзаголовок |
| — Параграфы | Абзацы текста (можно добавлять/удалять) |
| — Цены | Пары «Название — Стоимость» |
| — Список пунктов | Буллет-лист (если есть) |
| CTA-секция | Нижний блок с кнопкой |

**Важно:** Количество услуг фиксировано (4 штуки). Для добавления или удаления обратитесь к разработчику.

---

### Контакты (`Контакты`)

| Поле | Описание |
|------|----------|
| Метка, Заголовок, Подзаголовок | Шапка страницы |
| Процесс записи | 4 шага: заголовок + текст каждого |
| CTA-секция | Нижний блок с кнопкой |

---

### Статьи (`Статьи`)

#### Список статей

Таблица со всеми статьями. Показывает название, статус (Опубликована / Черновик).

Кнопки:
- **«+ Новая статья»** — создать статью
- **Редактировать** — изменить существующую
- **Удалить** — удалить с подтверждением

#### Создание / редактирование статьи

| Поле | Описание |
|------|----------|
| Заголовок | Название статьи |
| Slug (URL) | Адрес в URL. Генерируется автоматически из заголовка, можно изменить вручную. Допускаются только латиница, цифры и дефис |
| Изображение | Обложка статьи (см. [требования](#обложка-статьи)) |
| Краткое описание | Текст для карточки в списке статей (1-2 предложения) |
| Содержание | Основной текст в формате HTML (см. [Форматирование текста](#форматирование-текста-статей)) |
| Опубликована | Галочка: если не отмечена, статья будет черновиком и не появится на сайте |

---

### Документы (`Документы`)

Управление сканами документов об образовании и квалификации.

#### Настройки страницы

| Поле | Описание |
|------|----------|
| Заголовок страницы | Текст `<h1>` на странице документов |
| Подзаголовок | Пояснение под заголовком |
| Текст кнопки | Текст кнопки на странице «Обо мне» |

#### Загруженные документы

Список уже добавленных документов:
- **Открыть** — просмотреть полноразмерное изображение
- **Удалить** — отметить для удаления (применяется после «Сохранить»)
- Название — необязательная подпись под документом

#### Добавить документы

- Нажмите на зону загрузки или перетащите файл
- Можно добавить подпись (необязательно)
- Кнопка **«+ Ещё документ»** — добавить ещё один файл за раз

**Кнопка на странице «Обо мне» появляется автоматически** после загрузки хотя бы одного документа.

---

## Требования к изображениям

### Общие требования

| Параметр | Значение |
|----------|----------|
| **Допустимые форматы** | `.jpg`, `.jpeg`, `.png`, `.webp`, `.gif`, `.svg` |
| **Максимальный размер файла** | 16 МБ |
| **Рекомендуемый формат** | `.webp` (лучшее сжатие) или `.jpg` |

> **Совет:** Перед загрузкой оптимизируйте изображения — это ускорит загрузку сайта. Используйте сервис [squoosh.app](https://squoosh.app/) для сжатия без потери качества.

---

### Фото героя (Главная)

Основное фото на первом экране сайта.

| Параметр | Значение |
|----------|----------|
| Контейнер | 420 × 500 px (десктоп), 280 × 340 px (планшет), 220 × 280 px (телефон) |
| **Рекомендуемый размер** | **840 × 1000 px** (2x для Retina) |
| Пропорции | ~5:6 (вертикальное, портретная ориентация) |
| Обрезка | Изображение заполняет контейнер (`object-fit: cover`) — края могут обрезаться |
| Рекомендация | Портретное фото, голова и плечи в центре кадра, однотонный фон |

---

### Фото превью «Обо мне» (Главная)

Фото в блоке «Обо мне» на главной странице.

| Параметр | Значение |
|----------|----------|
| Контейнер | ~50% ширины × 400 px (десктоп), 100% × 400 px (планшет) |
| **Рекомендуемый размер** | **800 × 800 px** |
| Пропорции | Свободные, но лучше квадратные или горизонтальные |
| Обрезка | `object-fit: cover` — заполняет контейнер |
| Рекомендация | Фото за работой, в интерьере или портрет в другом ракурсе |

---

### Фото специалиста (Страница «Обо мне»)

Фото в шапке страницы «Обо мне».

| Параметр | Значение |
|----------|----------|
| Контейнер | 300 × 380 px (десктоп), 240 × 300 px (планшет) |
| **Рекомендуемый размер** | **600 × 760 px** (2x для Retina) |
| Пропорции | ~3:4 (вертикальное) |
| Обрезка | `object-fit: cover` |
| Рекомендация | Деловой портрет, верхняя часть тела |

---

### Обложка статьи

Изображение для карточки статьи в списке и в шапке статьи.

| Параметр | Значение |
|----------|----------|
| Контейнер карточки | Пропорции 16:9 |
| Контейнер статьи | 720 px по ширине |
| **Рекомендуемый размер** | **1440 × 810 px** (2x, пропорции 16:9) |
| Пропорции | 16:9 (горизонтальное) |
| Обрезка | В карточке — `object-fit: cover`; на странице — `width: 100%` |
| Рекомендация | Тематическое фото, иллюстрация, абстракция. Необязательно — без фото статья будет с серой заглушкой |

---

### Документы (сертификаты, дипломы)

Сканы или фотографии документов.

| Параметр | Значение |
|----------|----------|
| Контейнер | Пропорции 3:4 (вертикальные) |
| **Рекомендуемый размер** | **900 × 1200 px** |
| Пропорции | 3:4 (вертикальный A4-подобный формат) |
| Обрезка | `object-fit: cover` в карточке; по клику открывается полный размер |
| Рекомендация | Скан документа (200-300 dpi), ровный, без теней. Фото документа на белом фоне тоже подойдёт |

---

## Форматирование текста статей

Содержание статьи вводится в формате **HTML**. Основные теги:

### Параграфы
```html
<p>Текст абзаца.</p>
<p>Ещё один абзац.</p>
```

### Заголовки
```html
<h2>Большой подзаголовок</h2>
<h3>Маленький подзаголовок</h3>
```

### Выделение
```html
<strong>Жирный текст</strong>
<em>Курсив</em>
```

### Списки
```html
<ul>
    <li>Первый пункт</li>
    <li>Второй пункт</li>
    <li>Третий пункт</li>
</ul>
```

### Нумерованный список
```html
<ol>
    <li>Первый шаг</li>
    <li>Второй шаг</li>
</ol>
```

### Цитата
```html
<blockquote>Текст цитаты — будет выделен отступом и линией слева.</blockquote>
```

### Ссылка
```html
<a href="https://example.com">Текст ссылки</a>
```

### Пример полной статьи
```html
<p>Вводный параграф статьи. Кратко о чём пойдёт речь.</p>

<h3>Первый подраздел</h3>
<p>Основной текст с <strong>важными моментами</strong> и <em>акцентами</em>.</p>

<h3>Что вы получите</h3>
<ul>
    <li>Первый результат</li>
    <li>Второй результат</li>
    <li>Третий результат</li>
</ul>

<blockquote>Важная мысль, которую хочется выделить.</blockquote>

<p>Заключительный абзац. Подробнее — <a href="https://example.com">по ссылке</a>.</p>
```

---

## Публикация изменений

После внесения изменений нужно их опубликовать, чтобы они появились на публичном сайте.

### Способ 1: Кнопка «Опубликовать» (рекомендуется)

1. Внесите все нужные изменения в разных разделах
2. Проверьте результат, открыв сайт по ссылке «Открыть сайт» в меню
3. Нажмите **«Опубликовать»** в боковом меню
4. Дождитесь появления сообщения «Деплой завершён» (~1-2 минуты)
5. Обновления появятся на публичном сайте

### Что делает кнопка «Опубликовать»:

1. Собирает статическую версию сайта
2. Сохраняет изменения в Git
3. Отправляет в GitHub
4. GitHub автоматически публикует обновление

> **Важно:** Кнопка публикует **все** несохранённые изменения разом. Убедитесь, что все разделы выглядят корректно перед нажатием.

---

## Замечания

- **Изменения в админке видны сразу** на локальном сервере (после нажатия «Сохранить»)
- **На публичном сайте** изменения появляются только после нажатия «Опубликовать»
- **Количество услуг фиксировано** — 4 штуки. Для изменения обратитесь к разработчику
- **Slug статьи** лучше не менять после публикации — старая ссылка перестанет работать
- **Удаление изображения** — удаляет файл с сервера. Восстановить нельзя, потребуется загрузить заново
