# Развёртывание сайта

Проект поддерживает два режима работы:

| Режим | Назначение | URL |
|-------|-----------|-----|
| **Локальный сервер (Docker + Portainer)** | Админ-панель для редактирования контента | `http://<IP>:4343/admin/` |
| **GitHub Pages** | Публичная статическая версия сайта | `https://<user>.github.io` |

**Как это связано:** вы редактируете контент в админке на локальном сервере, нажимаете **«Опубликовать»** — и обновлённая версия автоматически появляется на GitHub Pages.

---

## 1. Локальный сервер (UmbrelOS + Portainer)

> Инструкция для UmbrelOS 1.5. Для другой ОС шаги аналогичны — отличается только способ SSH-подключения.

### 1.1. Схема работы

```
┌─────────────────────────────────────┐
│  UmbrelOS (домашний сервер)         │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  Portainer (:9000)            │  │
│  │                               │  │
│  │  ┌─────────────────────────┐  │  │
│  │  │  mironova-site (:4343)  │  │  │
│  │  │                         │  │  │
│  │  │  Flask + Gunicorn       │  │  │
│  │  │  ┌───────┐ ┌────────┐  │  │  │
│  │  │  │ /data │ │ .git   │  │  │  │
│  │  │  │(json) │ │(repo)  │──┼──┼──┼──→ GitHub → GitHub Pages
│  │  │  └───┬───┘ └───┬────┘  │  │  │
│  │  └──────┼─────────┼───────┘  │  │
│  └─────────┼─────────┼──────────┘  │
│            ↕         ↕              │
│    ~/mironova/data  ~/mironova/.git │
│    ~/mironova/static/uploads        │
│    ~/.ssh/  (ключи для GitHub)      │
└─────────────────────────────────────┘
```

Контейнер использует код из Docker-образа, а данные (JSON, загрузки, git) монтируются с хоста через volumes. Это значит:
- **Данные сохраняются** при пересборке образа
- **Код обновляется** при пересборке (`docker compose up --build`)
- **Кнопка «Опубликовать»** делает git push прямо из контейнера

### 1.2. Предварительные требования

- UmbrelOS 1.5 (или любой Linux-сервер с Docker)
- Portainer установлен (в UmbrelOS — через App Store)
- SSH-доступ к серверу
- Аккаунт на GitHub с подготовленным репозиторием

### 1.3. Подключение к серверу

```bash
# С вашего Mac / PC подключитесь к UmbrelOS
ssh umbrel@umbrel.local
# Пароль — тот, что вы задали при настройке UmbrelOS
```

> Если `umbrel.local` не резолвится, используйте IP-адрес сервера:
> `ssh umbrel@192.168.0.161` (замените на ваш IP)

### 1.4. Клонирование репозитория

```bash
# Создадим папку и клонируем (пока через HTTPS — SSH настроим позже)
cd ~
git clone https://github.com/<USER>/mironova.github.io.git mironova
cd mironova
```

> Замените `<USER>` на ваш GitHub-логин (например, `mironova-yu`).

### 1.5. Создание SSH-ключа для GitHub

SSH-ключ нужен, чтобы кнопка «Опубликовать» могла отправлять изменения в GitHub.

```bash
# 1. Создать ключ (нажмите Enter на все вопросы — пароль не нужен)
ssh-keygen -t ed25519 -C "umbrel-server"

# 2. Показать публичный ключ — скопируйте его
cat ~/.ssh/id_ed25519.pub
```

Теперь добавьте ключ в GitHub:

1. Откройте **[github.com](https://github.com) → Settings → SSH and GPG keys**
2. Нажмите **New SSH key**
3. Title: `Umbrel Server`
4. Key: вставьте скопированный ключ (начинается с `ssh-ed25519 ...`)
5. Нажмите **Add SSH key**

Проверьте соединение:
```bash
ssh -T git@github.com
# Должно появиться: "Hi <USER>! You've successfully authenticated..."
```

Теперь переключите remote на SSH:
```bash
cd ~/mironova
git remote set-url origin git@github.com:<USER>/mironova.github.io.git

# Проверить
git remote -v
# origin  git@github.com:<USER>/mironova.github.io.git (fetch)
# origin  git@github.com:<USER>/mironova.github.io.git (push)
```

### 1.6. Настройка переменных окружения

```bash
cd ~/mironova

# Создать .env из шаблона
cp .env.example .env

# Редактировать
nano .env
```

Заполните:

```env
SECRET_KEY=<длинный-случайный-ключ>
ADMIN_PASSWORD=<пароль-для-админки>
GIT_USER_NAME=Имя Фамилия
GIT_USER_EMAIL=email@example.com
```

Сгенерировать ключ:
```bash
python3 -c "import secrets; print(secrets.token_hex(32))"
```

> Если python3 не установлен: `openssl rand -hex 32`

Сохраните файл: `Ctrl+O`, `Enter`, `Ctrl+X`.

### 1.7. Запуск через CLI (рекомендуется для UmbrelOS)

```bash
cd ~/mironova

# Собрать образ и запустить контейнер
docker compose up -d --build

# Проверить, что контейнер работает
docker compose ps
```

Ожидаемый вывод:
```
NAME             STATUS         PORTS
mironova-site    Up 2 minutes   0.0.0.0:4343->4343/tcp
```

После запуска откройте в браузере (с любого устройства в локальной сети):

- **Сайт:** `http://umbrel.local:4343`
- **Админка:** `http://umbrel.local:4343/admin/`

> Или по IP: `http://192.168.0.161:4343/admin/`

Контейнер автоматически появится в Portainer → **Containers** для мониторинга.

### 1.8. Альтернатива: запуск через Portainer (GUI)

Если предпочитаете настроить через веб-интерфейс Portainer:

1. Откройте **Portainer** → **Stacks** → **Add stack**
2. Название: `mironova-site`
3. Build method: **Repository**
   - Repository URL: `https://github.com/<USER>/mironova.github.io`
   - Reference: `refs/heads/main`
   - Compose path: `docker-compose.yml`
4. В разделе **Environment variables** добавьте:

   | Name | Value |
   |------|-------|
   | `SECRET_KEY` | длинный случайный ключ |
   | `ADMIN_PASSWORD` | пароль для входа в админку |
   | `GIT_USER_NAME` | ваше имя для git |
   | `GIT_USER_EMAIL` | ваш email для git |
   | `GIT_REMOTE_URL` | `git@github.com:<USER>/mironova.github.io.git` |

   > `GIT_REMOTE_URL` нужен, потому что Portainer клонирует репо через HTTPS, а для push нужен SSH. Entrypoint при старте автоматически подменит remote.

5. Нажмите **Deploy the stack**

### 1.9. Управление контейнером

**Через терминал (SSH):**
```bash
cd ~/mironova

docker compose logs -f --tail=50 web   # посмотреть логи
docker compose restart                  # перезапустить
docker compose down                     # остановить
docker compose up -d --build            # пересобрать и запустить
```

**Через Portainer:**
**Containers** → `mironova-site` → кнопки **Start / Stop / Restart / Logs**

---

## 2. Кнопка «Опубликовать» — как это работает

### 2.1. Что происходит при нажатии

Кнопка находится в боковом меню админки. При нажатии запускается фоновый процесс из 4 шагов:

```
1. 🔨 Сборка    → freeze.py генерирует статический HTML из текущего контента
2. 📦 Коммит    → git add -A + git commit (фиксирует изменения в data/ и uploads/)
3. 🚀 Push      → git push origin main (отправляет в GitHub)
4. ⚡ Actions    → GitHub Actions автоматически разворачивает сайт на Pages (~1-2 мин)
```

Прогресс отображается в реальном времени прямо в админке.

### 2.2. Что для этого нужно

Чтобы кнопка работала, контейнеру нужны:

| Компонент | Volume в docker-compose | Зачем |
|-----------|------------------------|-------|
| Git-репозиторий | `./.git:/app/.git` | `git commit` + `git push` |
| Git-ignore | `./.gitignore:/app/.gitignore` | `git add` знает что пропускать |
| SSH-ключи | `~/.ssh:/root/.ssh:ro` | Авторизация на GitHub |
| Данные | `./data:/app/data` | JSON-файлы которые коммитятся |
| Загрузки | `./static/uploads:/app/static/uploads` | Изображения которые коммитятся |

Переменные окружения:

| Переменная | Зачем |
|-----------|-------|
| `GIT_USER_NAME` | Имя автора в git commit |
| `GIT_USER_EMAIL` | Email автора в git commit |
| `GIT_REMOTE_URL` | *(опционально)* Переключает remote на SSH, если репо клонировано через HTTPS |

### 2.3. Проверка работоспособности

```bash
# 1. Проверить, что .git доступен в контейнере
docker exec mironova-site git -C /app status

# 2. Проверить SSH-ключ
docker exec mironova-site ls /root/.ssh/

# 3. Проверить доступ к GitHub
docker exec mironova-site ssh -T git@github.com

# 4. Проверить remote (должен быть git@github.com:...)
docker exec mironova-site git -C /app remote -v
```

Если всё ок — кнопка «Опубликовать» готова к работе.

### 2.4. Диагностика проблем

**«Ошибка git push»:**
```bash
# Проверьте remote — должен быть SSH, не HTTPS
docker exec mironova-site git -C /app remote -v

# Если показывает https://, исправьте:
docker exec mironova-site git -C /app remote set-url origin git@github.com:<USER>/mironova.github.io.git

# Или добавьте GIT_REMOTE_URL в .env / переменные стека
```

**«Permission denied (publickey)»:**
```bash
# Убедитесь, что ключ на хосте существует
ls ~/.ssh/id_ed25519

# Убедитесь, что он привязан к GitHub
ssh -T git@github.com

# Убедитесь, что он доступен из контейнера
docker exec mironova-site ssh -T git@github.com
```

**«No such file or directory: /app/.git»:**
Volume `./.git:/app/.git` не смонтировался. Убедитесь, что запуск идёт из директории с клонированным репозиторием.

---

## 3. GitHub Pages (публичный сайт)

### 3.1. Автоматический деплой

Деплой происходит автоматически при каждом `git push` в ветку `main`:

1. GitHub Actions запускает workflow `.github/workflows/deploy.yml`
2. Устанавливает Python и зависимости
3. Запускает `python freeze.py` — создаёт статическую версию
4. Публикует папку `build/` на GitHub Pages

### 3.2. Настройка GitHub Pages (один раз)

1. Перейдите в **GitHub → Settings → Pages**
2. В разделе **Source** выберите: **GitHub Actions**
3. Сохраните

### 3.3. Ручной деплой (без кнопки, для разработчика)

```bash
cd ~/mironova

python freeze.py          # Сборка статики в build/
git add -A
git commit -m "Обновление контента"
git push origin main
```

---

## 4. Структура проекта

```
mironova.github.io/
├── app.py                  # Flask-приложение + админ-панель
├── freeze.py               # Генерация статического сайта
├── requirements.txt        # Python-зависимости
├── Dockerfile              # Docker-образ
├── docker-compose.yml      # Docker Compose конфигурация
├── docker-entrypoint.sh    # Скрипт запуска контейнера
├── .env.example            # Шаблон переменных окружения
├── .github/
│   └── workflows/
│       └── deploy.yml      # GitHub Actions — авто-деплой
├── data/
│   ├── content.json        # Все тексты и настройки сайта
│   └── articles.json       # Статьи блога
├── static/
│   ├── css/
│   │   ├── style.css       # Стили сайта
│   │   └── admin.css       # Стили админ-панели
│   ├── js/
│   │   └── main.js         # Скрипты сайта
│   ├── images/             # Начальные изображения
│   └── uploads/            # Загруженные через админку файлы
│       ├── pages/          # Фото страниц (герой, обо мне)
│       ├── articles/       # Обложки статей
│       └── documents/      # Сканы документов
└── templates/
    ├── base.html           # Базовый шаблон
    ├── index.html          # Главная
    ├── about.html          # Обо мне
    ├── services.html       # Услуги
    ├── contact.html        # Контакты
    ├── articles.html       # Список статей
    ├── article.html        # Отдельная статья
    ├── documents.html      # Документы и сертификаты
    ├── 404.html            # Страница ошибки
    └── admin/              # Шаблоны админ-панели
        ├── base.html
        ├── dashboard.html
        ├── login.html
        ├── edit_site.html
        ├── edit_index.html
        ├── edit_about.html
        ├── edit_services.html
        ├── edit_contact.html
        ├── articles_list.html
        ├── edit_article.html
        └── edit_documents.html
```

---

## 5. Решение проблем

### Контейнер не запускается

```bash
docker compose logs web        # Посмотреть ошибку
docker compose down && docker compose up -d --build   # Пересобрать
```

### GitHub Pages не обновляется

1. Проверьте вкладку **Actions** в репозитории — должен быть запущен workflow
2. Убедитесь, что Source в Settings → Pages установлен на **GitHub Actions**
3. Проверьте, что `git push` прошёл успешно

### Порт 4343 занят

Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8080:4343"   # внешний:внутренний
```

### Загруженные файлы пропали после пересборки

Данные CMS (`data/`) и загрузки (`static/uploads/`) хранятся в примонтированных volumes. При **первом запуске** entrypoint автоматически копирует дефолтные данные из образа в пустые volumes. При пересборке образа данные в volumes сохраняются. Не удаляйте папку проекта на сервере.
